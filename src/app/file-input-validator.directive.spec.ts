import { FileValidatorDirective } from './file-validator.directive';

describe('FileValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
